# Projeto Elos - Rede de Apoio (Plataforma ONG)

Este é um projeto acadêmico para a disciplina de [Nome da Disciplina] do curso de Análise e Desenvolvimento de Sistemas. O objetivo é desenvolver uma plataforma web completa e profissional para uma ONG de doações, aplicando conceitos de HTML, CSS, JavaScript e práticas de desenvolvimento de mercado.

## 🚀 Visão de Design
O projeto "Elos" foi construído com uma estética **minimalista e moderna**, focada em transições suaves (CSS Transitions) e feedback claro ao usuário (JS). A paleta de cores (verde sálvia, branco e cinza) foi escolhida para transmitir calma, confiança e profissionalismo.

## 🛠️ Entregas e Funcionalidades

### Entrega 1: Estrutura HTML
* **HTML5 Semântico:** Estrutura organizada em `header`, `main`, `footer`, `section` e `article`.
* **Formulário Complexo:** Uso de `fieldset`, `legend`, `label` e tipos de input HTML5 com validação nativa (`pattern`, `required`).
* **Validação W3C:** Todo o código HTML foi validado (W3C Validator).

### Entrega 2: Estilização CSS
* **Design System:** Uso de Variáveis CSS (`:root`) para paleta de cores, tipografia e espaçamento.
* **Layout Moderno:** Uso de CSS Grid e Flexbox para um layout responsivo (Mobile-First).
* **Componentes Elegantes:** Cards, botões e inputs com transições suaves de `hover` e `focus`.

### Entrega 3: Interatividade JavaScript
* **Prevenção de Envio:** O erro 405 (HTTP) é evitado usando `event.preventDefault()` no envio do formulário.
* **Interações "Bonitas":**
    * Efeito "fade-in" em elementos ao rolar a página.
    * Sombra dinâmica no cabeçalho (`header`) ao rolar.
    * Botão "Voltar ao Topo" que aparece e tem scroll suave.
* **Validação de Formulário "Bonita":**
    * Verificação de consistência em tempo real (evento `blur`).
    * Mensagens de erro elegantes (sem `alert()`) baseadas nos atributos `title` do HTML.
    * Validação lógica do CPF (dígito verificador).

### Entrega 4: Práticas Profissionais
* **GitFlow:** Uso de branches (`entrega-4`) para desenvolvimento de features.
* **Acessibilidade (WCAG):** Estados de foco visíveis, `label` e `alt` corretos, e suporte a `prefers-color-scheme` (Modo Escuro).
* **Otimização:** Arquivos CSS e JS "minificados" (`.min.css`, `.min.js`) para melhor performance.

## 👤 Autor
* **Guilherme Boutin**
* [seu-linkedin.com/in/seu-usuario]